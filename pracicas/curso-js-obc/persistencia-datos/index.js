let nombre = "Eze"

let apellido = "Borbotones"

const fullName = {
    nombre,
    apellido
}

// sessionStorage.setItem("Nombre completo",JSON.stringify(fullName));

// localStorage.setItem("Nombre completo",JSON.stringify(fullName));

// document.cookie = "nombreCompleto=" + JSON.stringify(fullName) + ";expires=" + new Date(Date.now()+120000).toUTCString()